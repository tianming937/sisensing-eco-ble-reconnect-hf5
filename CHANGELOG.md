# Changelog

## v16.1-hf5 — 2026-08-09

- 修复 `V1.1.2E`、`E1.1.2G`、`E1.1.5M` 三套 `AlgorithmContext.toString()` 残缺路径。
- 保留原本正常的 `E1.1.5N` 算法路径。
- 算法工厂在返回实例前进行类加载、实例化和 `toString()` 预检，并捕获所有 `Throwable`，包括 `VerifyError`。
- 未知、空、`null` 或预检失败的算法返回 `null`，不再回退到错误的默认算法。
- BLE 释放路径允许算法对象为空，避免保护分支退出时触发 Kotlin 非空断言崩溃。
- 继承 hf4 的亮屏全通过扫描、灭屏多过滤器扫描和 BLE 自动恢复。
- 版本升级为 `versionCode=37`、`versionName=02.23.01.00-v16.1-hf5`。

## v16.1-hf4 — 2026-08-09

- 修复把探头末四位软件匹配键错误地当成 Android 精确设备名过滤器的问题。
- 亮屏恢复全通过扫描；灭屏使用完整名称、短名称和服务 UUID 三个 OR 过滤器。
- 保留 SDK 回调中的末四位二次匹配，避免仅凭服务 UUID 连接错误设备。

## v16.1-hf3 — 2026-08-08

- 修复部分 Android 版本上的界面类校验崩溃。

## v16.1-hf2 — 2026-08-08

- 修复获取短信验证码时的类校验崩溃。

## v16.1 — 2026-08-08

- 增加灭屏非空扫描过滤器和有限时长 WakeLock。

## v16 — 2026-08-08

- 增加 BLE 重连监督器、有界退避、过渡态看门狗、数据健康检查和隐私安全诊断。
