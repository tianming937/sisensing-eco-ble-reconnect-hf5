# 安全策略

## 报告安全问题

涉及签名材料、账号接管、敏感健康数据泄漏、远程代码执行或可导致错误血糖输出的问题，不要直接发布完整利用细节和真实数据。请通过 GitHub Security Advisory 的私密报告入口联系维护者；若仓库暂未启用该功能，只提交一个不含敏感细节的 Issue，请求私下沟通渠道。

## 支持范围

当前维护版本：`v16.1-hf5`。

以下不属于安全漏洞：

- 极弱信号、超出蓝牙范围或探头停止广播导致的暂时无数据；
- Android 因签名不同拒绝覆盖安装；
- 未知未来算法被安全拒绝；
- 本项目明确列出的非官方、非医疗器械边界。

以下应按高优先级报告：

- 未知算法被错误映射为某个已知算法并输出数值；
- 诊断日志泄露血糖、账号、令牌、MAC 或完整探头编号；
- 重连逻辑形成无上限扫描/连接风暴；
- App 显示已连接但长期输出缓存旧值且无任何保护或提示。

## 健康安全

如果发现数据缺失、异常或与身体症状不一致，请先使用可靠的备用测量方式处理健康风险，再收集软件证据。不要为了复现问题反复关闭蓝牙、清除 App 数据或破坏正在使用的传感器会话。
